# Global Agent Instructions

These instructions apply to ALL Claude Code sessions.

## Rules
- Always respond in Portuguese
- Follow coding best practices
- Run tests after making changes when possible

## Active Technologies
- Python 3.12 + FastAPI, asyncpg, httpx, openai, pydantic, pydantic-settings, PyMuPDF, beautifulsoup4, jsonschema, PyYAML, alembic, SQLAlchemy (models only) (002-processing-engine)
- PostgreSQL 16 with pgvector extension (schema: `processing_engine`) (002-processing-engine)

## Recent Changes
- 002-processing-engine: Added Python 3.12 + FastAPI, asyncpg, httpx, openai, pydantic, pydantic-settings, PyMuPDF, beautifulsoup4, jsonschema, PyYAML, alembic, SQLAlchemy (models only)
